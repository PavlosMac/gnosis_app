# Work Log

This file logs work completed on the project. Keep it simple - just enough to remember what was done.

## Format

Each entry should include:
- Date (YYYY-MM-DD)
- Brief description (1-2 lines)
- Status (completed, in-progress, blocked)
- Notes (optional)

---

## Entries

<!-- Add new work log entries below this line -->

- **2026-07-09** — Step 1 frontend of decoupled reading/interpretation flow (spec `docs/superpowers/specs/2026-07-08-decoupled-interpretation-flow-design.md`): ribbon save via new `createReading`, generate/preview/save `InterpretationModal`, nested `ReadingDetail.interpretation` with null branch + restore on detail page, vitest setup. Status: completed (manual FE+BE flows verified).


- **2026-08-21** — Multi-lens interpretations (plan `docs/multi-lens-interpretations.md`): replaced style/depth/tone/context tuning with lens (traditional/psychological/esoteric/alchemical) + intent (reflective/predictive) + depth %, one saved interpretation slot per lens (max 4, `PUT /interpretations/{lens}` upsert with replace confirm), lens tabs on reading detail, sticky defaults in localStorage, backup/restore feature removed. Status: completed (FE; awaiting matching FastAPI endpoints).

- **2026-08-21** — Code-review fixes on `update-interpreter`: catch blocks around generate/save server actions in `InterpretationModal` and `TarotGame` (stuck-spinner/stuck-saving states), stale-createReading generation guard, readingId format validation in interpret actions, `interpretations ?? []` fallback in `getReading` for legacy readings. Status: completed (tsc + 17 tests green).

- **2026-08-21** — Reading Style modal (plan `docs/manual-entry-flow.md`, Feature 2): fixed top-right "◈ Reading Style" button in `TarotGame` opens new `ReadingStyleModal` (reuses `InterpretationSettingsControls`); settings seeded from/persisted to the localStorage sticky default; `InterpretationModal` gains `initialSettings`/`autoGenerate` props so the game flow generates immediately (journal page keeps tweak-first). Status: completed (tsc, tests, build green). Feature 1 (manual reading page) still planned in same doc.

- **2026-08-25** — Session-expired save path: `generate`/`save` results carry `unauthenticated` (no user or 401); `InterpretationModal` swaps Save/Try Again/close-confirm for "Log In (to Save)" + "Leave", stashing the unsaved result in sessionStorage (`interpretation-stash.ts`); login honours a validated `?from=` path (proxy already set it) and the reading detail page reopens the modal in preview from the stash. Status: completed (tsc, 22 tests, build green).

- **2026-08-25** — Manual Interpretation (plan `docs/manual-entry-flow.md`, Feature 1): profile link → `/user/manual-reading` renders `<TarotGame mode="manual">`; new `manual-select` reducer phase + `MANUAL_*` actions (`buildReading` shared with `SELECT_CARD`, `gameReducer` exported); new `FaceUpDeck` (deck in `MANUAL_DECK_ORDER`, sticky tray with per-card reverse/remove, Reveal lands straight in `reading`); Significators + reversals pill hidden in manual mode; save/interpretation flow reused unchanged. Status: completed (tsc, 35 tests, build green). Note: `npm run lint` is broken repo-wide (`next lint` removed in Next 16 / legacy eslintrc) — pre-existing.

- **2026-08-25** — Game screen fixes: "◈ Reading Style" button is inline/centered under the subtitle on mobile (absolute top-right on `sm+`, hidden on mobile while a deck is on screen); saving an interpretation from the game now redirects to `/user/readings/[id]` so the journal page (lens tabs, "New Interpretation") takes over instead of the stale Oracle Interpretation button re-generating on the same lens. Status: completed (tsc, tests, build green).

- **2026-08-26** — Journal detail page now lays out the saved spread like the game: `InterpretationSection` gained a `SpreadCards` block that uses `KabbalahLayout` when `isTreeOfLife(positions)` (exported from `Reading.tsx`) and the flat card row otherwise; shown both before an interpretation exists and above the saved interpretation. Status: completed (tsc + build green).

- **2026-08-26** — New spreads in `readings-config.json`: Open Question 3 cards (no positions), Relationship Reading (9 cards, 3×3 pillars querent / relationship / other) and Career Reading (5 inline, ends with Action steps). `ReadingConfig`/`PositionConfig` moved to `src/types/reading.ts` with `description` and `positions` now optional; `resolvePositions()` (`src/lib/reading-positions.ts`) generates "Card 1..N" positions for position-less spreads so keying/saving/interpretation work unchanged. New `RelationshipLayout` selected by `isRelationship(positions)` in `Reading.tsx` and in the journal `SpreadCards`. Status: completed (tsc + build green; `npm run lint` still broken pre-existing). Gotcha: `cards` must equal `positions.length` and names must be unique — mismatches silently break manual reveal / save.

- **2026-08-27** — Post-review fixes for the Relationship spread: pillar constants + `isRelationship()` + `groupByPillar()` moved to `src/lib/relationship-spread.ts` (single source of truth); `RelationshipLayout` now places cards by position-name suffix rather than index, so a missing/reordered card can't shift into the wrong pillar; config descriptions for the middle pillar corrected to match their names; the two open-question spreads renamed to unique names ("Two/Three Card Open Question") since `name` is the key, select value and saved `spread_name`. Added `src/components/__tests__/Reading.layout.test.tsx` (SSR render + detection). Status: completed (tsc, 39 tests green).

- **2026-08-28** — `/code-review xhigh` on the above found the pillar fix was incomplete: `groupByPillar` grouped by pillar but still slotted cards by *order of appearance* within the pillar, so a missing middle-row card (e.g. a saved reading where `findCardByNameSafe` can't resolve one name) still shifted the remaining cards up a row instead of leaving a gap — rows misaligned across pillars. Fixed by also matching a **row** key (`current` / `what is desired` / `how to proceed`, matched by prefix) so `groupByPillar` returns a fixed 3×3 slot grid instead of an append-order list; `isRelationship` now requires every pillar/row slot filled. Added a regression test asserting row order survives a dropped card. Also fixed: `TarotCard`'s "(Reversed)" caption was showing on small cards too (duplicating the orientation pill `InterpretationDisplay` already renders) — restored the `!small` guard; and the new question `<textarea>` allows line breaks that were being silently collapsed on render (`Reading.tsx`, `InterpretationDisplay.tsx`, journal preview) — added `whitespace-pre-wrap`/`pre-line`. Status: completed (tsc, 40 tests, build green). Remaining lower-priority findings from that review (fragile `.endsWith` pillar match on unusual renames, no shared layout-selector, missing footer divider on `RelationshipLayout`, minor perf/duplication nits) deferred — not blocking.

- **2026-08-28** — Code-review fixes for the Relationship spread. (1) Reverted the two-card spread's name to "Open Question Reading" in `readings-config.json`: `spread_name` is persisted at draw time and `ReadingsFilterPanel` builds its pills from the config, so renaming it to "Two Card Open Question" made every previously saved two-card reading unfilterable. Only the new three-card spread needed a distinct name. (2) `showMeaning` was dead on `RelationshipLayout` — `TarotCard` gates the meaning on `!small && showMeaning`, so it never rendered; prop dropped and `showMeaning={false}` passed explicitly, matching `KabbalahLayout` (nine meanings would bury the spread). The `(Reversed)` caption was gated on `!small` too, which left rotation as the only cue in both small-card layouts — now shown for small cards as well (also fixes Tree of Life). (3) Small `TarotCard` is `w-20 h-32` below `sm:` (was a fixed `w-28`/112px against ~98px columns at 320px, so pillars collided under `overflow-clip`); measured at a 316px viewport, 98px columns vs an 80px card. (4) Follow-on: `RelationshipLayout` is now one flat grid of 3 headers + 9 cells in row order instead of three independent flex columns, so rows stay aligned when a label wraps or a "(Reversed)" caption appears in only one pillar, and a pillar missing a card renders an empty cell instead of pulling the rest up; cell keys are `row-col` rather than the position name. Status: completed (tsc clean, 39 tests green, verified in the browser). Note: `/reading` logs a pre-existing hydration mismatch in `ReadingPage` (`<main>` vs its child `<div>`) — unrelated to these files, most likely browser-extension DOM injection.

- **2026-09-01** — New Interpreter v2 (plan `docs/new-interpreter-v2.md`): settings collapsed to `{intent}` (lens/depth removed from types, zod schemas, UI and wire; word budget now server policy), display is a single "Reading Interpretation" narrative rendering `reading ?? synthesis` with the spread laid out above it via new shared `src/components/SpreadCards.tsx` (extracted from `InterpretationSection`); reading page shows only the newest saved interpretation (lens tabs gone); save PUTs to singular `/interpretation`; sessionStorage login-stash and replace-confirm overlay deleted (session-expired → plain login link via `loginHrefFor`); sticky default is now just the intent (`readDefaultIntent`/`writeDefaultIntent`); `ReadingStyleModal`/`InterpretationSettingsControls` are intent-only. Status: completed (tsc, 32 tests, build green; `npm run lint` still broken pre-existing). Generate/save 422 against today's backend by design — e2e waits for the lean gnosis backend.

- **2026-09-02** — Intent made optional + per-spread allowlist: `InterpretationSettings.intent` is now optional (types + zod); new `INTENT_SPREADS` allowlist and `spreadAllowsIntent()` in `src/lib/interpretation-defaults.ts` (Single Card, Open Question Reading, Three Card Open Question, Relationship Reading — all others have a fixed reading style). The "◈ Reading Style" button in `TarotGame` renders locked (disabled, 🔒) for fixed-style spreads; `InterpretationModal` sends `settings: {}` for them, hides the intent controls/caption/Tune & Regenerate, and shows "This spread has a fixed reading style." Status: completed (tsc, 36 tests, build green).

- **2026-09-02** — Intent removed entirely (plan `docs/remove-interpretation-intent.md`): deleted `ReadingStyleModal.tsx`, `InterpretationSettingsControls.tsx`, and `interpretation-defaults.ts` (+ its test) outright; `InterpretationSettings`/`intent`/`settings` dropped from `types/interpret.ts` and `saveInterpretationSchema`; `generateInterpretation(readingId)` now takes no settings param and POSTs with no body. `InterpretationModal`'s state machine simplifies to `confirm → generating → preview/saving/error` (renamed from `tweak`), with no regenerate path — a plain "Consult the Oracle" button replaces all settings UI, and the intent caption is gone from both the modal and the journal detail page. `TarotGame` loses the "◈ Reading Style" button, its modal, and all associated state. Status: completed (tsc, 26 tests, build green).

- **2026-09-03** — `/code-review` fixes on `update-interpreter-v2`: `saveInterpretationSchema` now `.refine()`s that at least one of `reading`/`synthesis` is non-empty (a 200 with no narrative content no longer silently saves); `InterpretationModal.runGenerate` treats an empty-narrative generate result as an error rather than showing a blank preview; extracted `src/lib/interpretation-narrative.ts` (`narrativeOf()`) as the single `reading ?? synthesis` accessor, replacing duplicated inline fallbacks in `InterpretationModal.tsx`/`InterpretationSection.tsx`; `SpreadCards` now derives its position list from `cardVisuals`' own keys instead of taking a separately-computed `positions` prop from each caller (removed the prop from `SpreadCards`/`InterpretationDisplay`, and the now-unused `cards` prop from `InterpretationSection`/its `page.tsx` caller); deleted dead `fake-storage.ts` test helper (its only two importers were removed by the intent-removal work). Status: completed (tsc, 27 tests, build green).

- **2026-09-03** — Wired the frontend to the landed lean backend (`gnosis-esoterica-api` branch `new-prompt-architecture`, ADR-005) after a live 404 on the old `/generate` path: `generateInterpretation(readingId)` now POSTs `/api/v1/readings/{id}/interpretation` (generate-and-persist, idempotent) and returns `{data, remainingBudgetUsd}`; deleted `saveInterpretation`, `saveInterpretationSchema`, and `interpretation-narrative.ts` (backend guarantees `reading`, so no `?? synthesis` fallback); `Interpretation` type mirrors `InterpretationReadModel` (`usage` ledger replaces flat `tokens_used`); `ReadingDetail.interpretation` singular; `InterpretationModal` collapses to confirm→generating→result/error with a Done button and shows remaining budget (no save step, no close-confirm); journal page drops the pointless "New Interpretation" button and shows model + token count from `usage`; 402 → "Your Oracle budget is exhausted." in `api-client.ts`. Verified: tsc, 29 tests, build green; live backend answers the new path (401 unauth as expected) and 404s the old one. E2e confirmed same day: a real interpretation generated and rendered on `/user/readings/[id]` with model (resolved snapshot id) + token footer (prompt+completion; reasoning tokens are a breakdown *within* completion per the OpenAI usage shape, so the sum is the full billed total).

- **2026-09-03** — Spread layout preview on `/reading` (plan `~/.claude/plans/on-the-reading-oracle-cozy-haven.md`): new `src/components/SpreadPreview.tsx` shows the selected spread's shape at setup time as miniature dashed-gold empty slots with position names, in the same geometry the real reading uses — Tree of Life stacked sephiroth rows (local `TREE_ROWS` constant mirroring `KabbalahLayout`, kept in sync by comment), Relationship 3×3 grid with pillar headers and suffix-stripped labels (reuses `groupByPillar`/`RELATIONSHIP_PILLARS`), wrap row for everything else (`resolvePositions` covers "Card 1..N" spreads); Significators renders its 4 birthdate positions plus a "Derived from your birth date" note. `TarotGame`'s setup block became a two-column shell: form column (`lg:flex-1 lg:max-w-2xl`) + `hidden lg:flex` right aside (`lg:w-64`), with a second `lg:hidden` instance under the spread description for mobile — the component is mounted twice since flex order can't interleave into mid-column. Panel re-mounts (`key={reading.name}`) with `animate-fadeIn` on selection change; slots stagger via inline `fadeIn ... backwards` (the `.animate-fadeIn` class has no backwards fill). Zero edits to `Reading`/`KabbalahLayout`/`RelationshipLayout`. Added `SpreadPreview.layout.test.tsx` (5 tests, `spread-preview-slot` marker class for counting). Status: completed (34 tests, build green; verified in headless Chrome at 1440/390px — desktop aside, mobile stacking, Tree + Relationship shapes all correct). Note: an apparent horizontal overflow at 390px in headless Chrome turned out to be a tooling artifact — headless Chrome clamps its window to a 500px minimum and `--screenshot` crops to the requested width; with proper CDP device emulation (`Emulation.setDeviceMetricsOverride`, 360px) the page has **no** horizontal scroll (`scrollWidth === innerWidth`, no element wider than the viewport).

- **2026-09-03** — Spread preview v2: promoted to a standalone sibling card right of the oracle card (user request; the v1 in-card aside made the setup form sit left-of-center). TarotGame's setup block reverted to its original centered single column; the outer return now wraps the main card in a `flex lg:flex-row` shell with a conditional `<aside className="hidden lg:block w-72 shrink-0 lg:sticky lg:top-6">` rendering `SpreadPreview` **only during the setup phase** — other phases have one flex child and `lg:justify-center` restores the centered card (`mt-16` moved to the wrapper; main card gains `lg:flex-1 lg:min-w-0`). `SpreadPreview` chrome upgraded to stand alone (0.95-opacity card gradient + radial gold glow overlay + shadow-2xl); mobile keeps the v1 inline instance under the description. Closable behavior explicitly deferred by the user. Status: completed (34 tests, build green; verified in real Chrome via CDP: aside right of card, form re-centered, 11 Tree slots, aside gone + card re-centered after Begin).

- **2026-09-03** — Spread preview v3 (user feedback on v2): the sibling card now sits flush beside the oracle card and always matches its height. The gap (previously ~100px of dead space) came from the main card's `mx-auto` absorbing the flex row's free space as auto margins — now `lg:mx-0` so only the wrapper's `gap-6` (24px) separates the cards. Height alignment via `lg:items-stretch` on the row (was `lg:items-start`) + `h-full` through SpreadPreview's card and content divs, with the shape area in a `flex-1 justify-center` wrapper so small spreads center vertically; sticky removed (meaningless under stretch). Because flex stretch sizes both children to the row's tallest item, the pairing holds in manual mode too (a short form card grows to fit the Tree preview instead of the preview overflowing). Mobile inline instance untouched. Status: completed (34 tests, build green; measured in real Chrome: gap 24px, both cards 664px for PPF and Tree of Life, tree content 660px fits).

- **2026-09-04** — Spread preview v4 (user request): only Tree of Life keeps the side panel; every other spread renders below the oracle card as a full-width horizontal "wide" card with bigger slots. `SpreadPreview` gained `variant: 'panel' | 'wide'` (panel = side aside + mobile inline, unchanged) and MiniSlot's `compact` boolean became `size: 'sm'|'md'|'lg'` (`lg` = `w-14 sm:w-20` + `text-[10px]` labels for wide default shape; Relationship wide uses `md` slots in a roomier `gap-x-10` grid). `TarotGame` dispatches on `isTreeOfLife(positionNames)` (existing memo + existing Reading.tsx export): tree → the v3 stretch aside; else → `hidden lg:flex max-w-6xl mt-6` wrapper under the flex row with `variant="wide"`. Status: completed (34 tests green; verified in real Chrome: PPF/Relationship below at 1152px width, slots 80px vs 44 in the old panel, Tree keeps side; wide card sits below the fold on laptop heights — acceptable per requested placement, revisit if discoverability suffers).

- **2026-09-04** — Spread preview v4.1: Relationship Reading moved to the side panel alongside Tree of Life (`isTreeOfLife(positionNames) || isRelationship(positionNames)` gates the aside; the below-card wide variant now serves only the small spreads). Verified in real Chrome: Relationship + Tree side, Career below; 34 tests green.

- **2026-09-04** — Spread preview v5: open/close toggle. New "✦ Spread Layout" button (Cinzel caps, gold /50→/90 hover, rotating SVG chevron, `aria-expanded`) under the spread description in `TarotGame`; gates all three preview mounts (side aside, below wide card, mobile inline) via one `showPreview` state. Persisted as `tarot:spread-preview-open` in localStorage — written on toggle, read in a mount effect (not the useState initializer, which would hydration-mismatch); storage access wrapped in try/catch. Status: completed (34 tests green; verified in real Chrome: close → 0 slots, reload keeps closed, reopen restores with fadeIn).

- **2026-09-04** — Spread preview v5.1: closed by default — `showPreview` initializes `false`; the mount effect now opens only when localStorage has `tarot:spread-preview-open === "true"`. Verified: fresh visitor sees only the toggle; an opened preview persists across reloads.

- **2026-09-04** — Spread preview v5.2: opening the toggle scrolls the preview card fully into view. The three mounts carry `data-spread-preview-anchor`; `togglePreview` (side effects moved OUT of the setState updater — StrictMode double-invokes updaters, was firing the scroll twice) schedules `scrollIntoView({behavior:'smooth', block:'nearest'})` on the visible anchor after `READING_SCROLL_DELAY`. Verified targeting with `behavior:'instant'` in the CDP-driven window (winY 0→77, card bottom flush with viewport); the smooth animation itself cannot run in an occluded automation window (no animation ticks — smooth scrollTo is equally inert there), which is a test artifact, not a product issue. 34 tests green.

- **2026-09-04** — Spread preview v5.3: changing the selected reading now also scrolls the (open) preview card fully into view — the scroll helper was extracted to `scrollPreviewIntoView()` and is called from both `togglePreview` and the spread `<select>` onChange (gated on `showPreview`), since a re-mounted card can be taller or swap between below/side placement. Verified via instrumented scrollIntoView in real Chrome: exactly one call per selection change, on the visible anchor. 34 tests green.

- **2026-09-07** — Custom pillar names for the Relationship Reading (plan `~/.claude/plans/i-would-like-to-mellow-liskov.md`): new `PillarNameInputs` (two lockable inputs, "✦ Who Is This Reading About? ✦", shown when Relationship is selected, both modes); `personalizePositionName`/`personalizeDescription` in `relationship-spread.ts` embed names into position strings ("Current behaviour (Alice) - querent" — suffixes kept so pillar detection and journal layout survive) and into descriptions for the backend LLM prompt; `pillarLabels` prop threads names into `SpreadPreview`/`Reading`/`RelationshipLayout` headers. Defaults untouched ⇒ flow byte-identical to before. Status: completed (tsc, 52 tests, build green; real-Chrome manual pass pending).

- **2026-09-07** — Pillar names round 2: display drops the querent/other keywords — `RelationshipLayout` now derives headers via new `extractPillarNames` (works for saved journal readings too, fixing their default headers) with bare captions via `stripPersonalizedSuffix`; `FaceUpDeck` tray shows "… - Pavlos" via `displayPositionName`; `pillarLabels` prop removed from `Reading`/`RelationshipLayout` (kept on `SpreadPreview`). Saving a named relationship reading auto-tags it with the lowercased names (`autoTagNames` + fire-and-forget `updateReadingTags` PATCH) so the readings list chips/tag filter convey who it's about. Unnamed readings byte-identical. Status: completed (tsc, 63 tests, build green; real-Chrome pass pending).

- **2026-09-08** — Investigated why interpretations name the "other" but not the querent: frontend payload verified correct (both names in positions + descriptions); cause is the backend's standard system prompt ("Address the querent directly" in `gnosis-esoterica-api/src/llm/prompt_builder.py`). Wrote a backend spec for a dedicated Relationship prompt variant (pillar structure + name-aware addressing): `gnosis-esoterica-api/docs/prompts/relationship-spread.md`. Status: spec written, backend implementation pending user review (cross-repo dependency).

- **2026-09-09** — Pillar names round 3: named readings now send keyword-free, template-built payloads — `buildNamedRelationshipPositions` emits `"Current behaviour - Alice"` / `"What is Alice's current behaviour?"` (no querent/other anywhere, so the LLM no longer treats the left pillar as "the asker"); regex generation (`personalizePositionName`/`personalizeDescription`) deleted. `groupByPillar` gained a name pass (positions sharing a `" - Name"` suffix form a pillar, first appearance = left; keyword + legacy `(Name) - querent` formats still match, so old saves render). `positionCaption` replaces `stripPersonalizedSuffix`; reserved/duplicate names are ignored by the game and flagged inline in `PillarNameInputs`. Status: completed (tsc, 65 tests, build green; real-Chrome + interpretation pass pending).

- **2026-09-09** — Spread preview always inline: removed the desktop side-aside and wide below-card preview mounts from `TarotGame` (and the `lg:flex-row` wrapper that hosted the aside — oracle card back to `max-w-6xl mx-auto mt-16`); the single inline mount under the "✦ Spread Layout" toggle now renders at every breakpoint, collapsed by default as before. `SpreadPreview` lost its `variant`/`wide` code paths; `scrollPreviewIntoView` simplified to one anchor. Status: completed (tsc, 65 tests, build green).

- **2026-09-09** — `/code-review` (high effort, 8 finder angles) on the branch, fixed the top 5 findings: (1) `keywordPillarIndexOf` matched the `other` keyword via substring `endsWith`, misclassifying custom names ending in "other" (Mother, Brother, Grandmother) as the keyword pillar instead of a named pillar — fixed by matching the whole trailing token after the first `" - "` instead (`trailingTokenOf`); the same first-vs-last-delimiter fix also covers (4) a leading `"- "` in a custom name corrupting through `nameSuffixOf`. (2) `ReadingsFilterPanel` pills came only from live config, so a reading renamed after being saved (e.g. "Open Question Reading" → "Two card Open Question Reading") vanished from the filter UI for old saved readings — now the URL's current `spreadType` is kept as a pill even when config no longer has it. (3) `handleNewReading` now resets `pillarNames` so stale custom names don't carry into a new reading. (5) the fire-and-forget tag-write catch now logs via the existing `[TAGS]` console convention instead of swallowing silently. Added regression tests for the keyword-collision and leading-dash cases. Status: completed (tsc, 70 tests, build green). Remaining findings (readings-config.json lost position descriptions on rename, surrogate-pair-unsafe tag truncation, three cleanup/dedup items) left open.

- **2026-09-09** — Tag autocomplete on the readings filter panel (plan `~/.claude/plans/i-think-the-way-streamed-minsky.md`, ADR-006): the tags filter is now a chip + autocomplete combobox (`TagFilterCombobox.tsx`) fed by the `user_tags` vocabulary (name + count, most-used first) already on the `GET /api/v1/readings` payload — no extra request. Focus lists the vocabulary, typing filters (prefix-first ranking via pure `filterTagSuggestions` in `src/lib/tag-suggestions.ts`, 14 unit tests), Arrows/Enter/click select, free text/comma/paste still commit chips, Apply serializes to the unchanged `tags=a,b` URL param. `parseTagsParam`/`serializeTags` extracted; `PaginatedReadings.user_tags`/`TagSummary` types added. Status: completed (tsc, 84 tests green; real-Chrome manual pass pending — needs backend with its working-tree user_tags changes running).

- **2026-09-09** — Tag autocomplete round 2 (user feedback after real-Chrome check): suggestions now require a 2-character query (`MIN_TAG_QUERY_LENGTH` in `tag-suggestions.ts` — no more list-all on focus); dropdown restyled black-on-parchment (`bg-[#e6d5b8]/95`, `text-black`, gold highlight `bg-[#d4af37]/40`); fixed dropdown hiding behind reading list items — the blurred panel container is a stacking context, so `z-50` inside it couldn't win; added `relative z-30` on the panel root. Status: completed (tsc, 83 tests green).

- **2026-09-10** — Filter context preserved on reading detail + prev/next navigation (plan `~/.claude/plans/i-think-the-way-streamed-minsky.md`, ADR-007): root cause of "back loses my filter" was the detail page's hardcoded back links, not browser history. List item links now carry `page`+filters (shared `parseListContext`/`listHref`/`readingHref` in `src/lib/reading-list-context.ts`; list page's local `PAGE_SIZE`/`isValidIsoDate` moved there); detail page accepts `searchParams`, all back affordances return to the filtered, correctly-paged list, and a new prev/next row (round gold ‹ › buttons + "Reading N of M", directly under the interpretation card) steps through the same filtered sequence — `getAdjacentReadings` in `readings/actions.ts` over pure `planAdjacency` (`src/lib/reading-adjacency.ts`), max 1 extra fetch at page edges, silent degradation when the reading left the filter. Direct visits (no params) get no prev/next and bare back links. Status: completed (tsc, 103 tests, build green; real-Chrome manual pass pending). Follow-up noted: proxy login-redirect `from` drops query strings (filtered-URL context lost across re-login).

- **2026-09-10** — Spread-type filter pills moved into a dropdown (space saving): new `src/components/SpreadTypeDropdown.tsx` — input-styled full-width trigger (selected spread as a gold pill, or muted "Any Spread"; rotating ▾ chevron) opening a dark absolutely-positioned panel holding the pill row verbatim (single-select, click-selected-to-clear, legacy-name pill, aria-pressed all preserved; `togglePill`'s Significators birth-date reveal untouched). Escape + click-away overlay close it; z-40 over in-panel siblings, panel root already z-30 over the list. `ReadingsFilterPanel` pill block replaced with the component. Status: completed (tsc, 103 tests green; real-Chrome pass pending).

- **2026-09-10** — Spread dropdown z-index bug root-caused in real Chrome via CDP (registered throwaway user zorder-test@example.com on the local dev backend to reach /user/readings): the popup's `z-40`/root `z-40` classes computed to `z-index: auto` — the long-running dev server's generated Tailwind CSS predated `SpreadTypeDropdown.tsx` and never contained the `z-40` utility, so the popup fell to z-auto (later Tags section painted above it) while the click-away overlay's `z-30` (already in the CSS) sat on top eating every click. Fix: layering z-indexes moved to inline `style={{ zIndex }}` (30 overlay / 40 root+popup) — immune to dev-CSS staleness; popup background made fully opaque. Verified via puppeteer-core against real Chrome: computed z 40, `elementFromPoint` inside popup, real mouse click selects a pill and closes; user confirmed. Note for future UI verification: screenshots of an **occluded** automation window can composite stale layers (phantom bleed-through) — `Page.bringToFront()` before screenshotting; this matches the earlier occluded-window scroll-animation artifact. Status: completed (tsc, 103 tests green).

- **2026-09-10** — Tag-suggestions dropdown went fully transparent after switching `bg-[#e6d5b8]/95` → `bg-[#e6d5b8]`: the long-running dev server's Tailwind regeneration missed the newly-introduced utility (same failure class as the z-40 incident; `text-black` was missing too, leaving parchment-on-parchment invisible text). Both dropdown panels now paint their critical presentation inline (`backgroundColor`/gradient, option `color`, highlight bg, zIndex) in `TagFilterCombobox.tsx`/`SpreadTypeDropdown.tsx` — immune to dev-CSS staleness; prod builds unaffected either way. Verified in real Chrome via CDP: solid parchment panel, black options with counts, covers Apply/Clear without bleed. Also added a CLAUDE.md styling rule per Pavlos: CSS must render similarly in Safari and Chrome (both supported equally); check computed styles before blaming engines. Status: completed (tsc, 103 tests green).

- **2026-09-11** — `/code-review` on the branch (8-agent fan-out stalled; findings came from a direct read of the diff). Fixed 2 of 3: (1) `PaginatedReadings.user_tags` was typed required but ADR-006 and the one call site (`?? []`) treat it as absent on un-migrated backends — now `user_tags?: TagSummary[]` so future call sites are forced to guard. (3) `SpreadTypeDropdown` never closed on Tab-away (only Escape/click-away) — added a wrapper `onBlur` that closes when `relatedTarget` is outside the component (a plain trigger blur would unmount the panel before a click/Tab into its focusable option buttons lands). Finding (2), prev/next buttons on the reading detail page round in Safari but not Chrome, is **deferred** by Pavlos; evidence so far rules out the usual suspects: not stale dev CSS (dev server restarted + hard reload, unchanged), `rounded-full` is in the served bundle as `border-radius: 3.40282e38px` (Lightning CSS folding of Tailwind v4's `calc(infinity * 1px)`), and local Chrome 152 accepts that value in an isolated headless test (computes to `3.35544e+07px`, still a circle on 36px). Caveat for future checks: Chrome's Computed tab lists only radius **longhands**, so filtering on `border-radius` shows nothing even when applied — filter on `radius`. Still needed: URLs open in each browser, computed `border-top-left-radius`/`width`/`height` in Chrome, and whether Styles shows `.rounded-full` struck through. Status: (1)+(3) completed (tsc, 103 tests green); (2) open. Side note: `npm run lint` is broken (`next lint` gone in Next 16) and `npx eslint` crashes on the legacy `.eslintrc` under ESLint 9 — lint tooling needs a flat-config migration.

- **2026-09-11** — Profile dashboard ("Your Sanctum", plan `docs/profile-dashboard.md`, ADR-008): `/user/profile` rebuilt as a two-panel card (`grid-cols-1 md:grid-cols-2`, `OrnateFrame` corners, gold gradient divider vertical ≥ md / horizontal below). Left `AccountPanel`: name, email, the Oracle budget as an animated SVG **chalice** (`src/components/BudgetChalice.tsx`, server component — liquid level = remaining/budget via a static clipPath, two counter-scrolling wave layers, shimmer sweep, glow pulse, rise-on-load; all keyframes in `tarot.css`, all stop under `prefers-reduced-motion`; dry-chalice state with dashed hairline; `role="img"` + `<title>`), caption "$2.41 of $3.00 remains", Manual Interpretation link, logout. Right `ReadingsPanel`: readings count, last reading card (→ `readingHref(id, null)`, "✦ Interpreted" marker), tag chips (→ `listHref({page:1, tags})`, 12 max + "+N more"), primary "New Reading" and outlined "All Readings". Data: one `GET /api/v1/dashboard` call via new `getDashboard()` server action (`profile/actions.ts`, replacing the dead re-export stub); `Dashboard`/`DashboardResponse` types + mapper in `types/auth.ts`; pure helpers in `src/lib/profile-dashboard.ts` (`chaliceFill`, `chaliceState`, `formatUsd`, `budgetCaption`, `formatReadingDate`, `truncateQuestion`, `tagHref` — the readings page now imports the shared date/truncate helpers). Failure of the dashboard fetch leaves account + links rendered with an "unavailable" line. The backend endpoint was built concurrently in the gnosis-esoterica-api session (this session reverted its own duplicate backend edits — frontend only). Status: completed (tsc, 122 tests, build green; verified in real Chrome via CDP at 1400px and 375px: fill levels 100/37/10/0%, animations on and off under reduced motion, chip click lands on the filtered list). Safari manual pass pending. Dev-CSS note: even a freshly started `npm run dev` served CSS lacking `space-y-8`, `mx-5`, `pt-8` for the new files — replaced with inline styles; `sanctum-*` layout classes live in `tarot.css` for the same reason.

- **2026-09-11** — Profile dashboard review fixes (code-integrity-reviewer, 3 findings, 0 critical): (1) a `$0`-capped account (`budget_usd: 0`, a real backend state) now draws the dry chalice instead of the chalice vanishing under an orphaned caption — `chaliceFill` treats `budget === 0` as fill 0 and only non-finite/negative figures as `null`; (2) `AccountPanel` hides chalice **and** caption together when `chaliceFill` is `null`, so a non-finite figure can no longer print "$NaN"; (3) `ProfileRow` value gets `minWidth: 0` so the `truncate` ellipsis actually fires on long emails in a flex row. Status: completed (tsc, 124 tests green).

- **2026-09-14** — Forgot/reset password flows (plan `~/.claude/plans/we-want-to-implmement-lexical-shell.md`): public root-level pages `/forgot-password` (email → `POST /api/v1/auth/forgot-password`; always-200 generic confirmation, 429 rate-limit copy is the only error) and `/reset-password?token=...` (route path is a backend contract — emailed links target `{FRONTEND_BASE_URL}/reset-password`; new+confirm password via `resetPasswordSchema` 8–128, hidden token input; 400→invalid/used panel, 410→expired panel, both link to `/forgot-password`; missing token renders the dead-link panel without POSTing; on 200 `clearAuthCookies()` — backend revokes all refresh tokens — then success panel links to login). Both outside the proxy matcher so no guard changes; reachable logged-in or out. "Forgot password?" link added under the login form (login page only, not `LoginToInterpretModal`). Profile dashboard: "Depart" logout button replaced by one-click "Renew the Seal" (`ResetPasswordRequestButton` + `requestPasswordResetForCurrentUser` in `profile/actions.ts` — email taken from `getCurrentUser()` server-side, never client input; success swaps button for inline confirmation, one shot per page view); logout remains in `ProfileNav`; unused `LogoutButton.tsx` deleted. Docs: auth-flow.md diagrams 8+9, CODE_INDEX hand-updated (note: `npm run code-index` needs universal-ctags — macOS BSD ctags empties the file; restored from git). Status: completed (tsc, 124 tests, build green; real-browser manual pass pending — needs backend with the password-reset endpoints + mail running, `FRONTEND_BASE_URL` set on the backend in prod).

- **2026-09-15** — Email service decided + backend contract written (ADR-009): Resend (free tier), sending centralized in the FastAPI backend, From `Tarot Divinations <noreply@tarotdivinations.com>`; apex-domain verification with DKIM/SPF records in Cloudflare DNS coexisting with Email Routing. Contract spec for the backend session: `docs/backend-contracts/email-service.md` (env vars, `send_email()` module, forgot-password email as first consumer, superadmin smoke-test endpoint). Status: docs completed; Resend signup/DNS + backend implementation pending (user / backend session).

- **2026-09-15** — Contact Support + manual-entry relocation (plan `docs/contact-support.md`, ADR-010): profile AccountPanel's "Get manual interpretation" row replaced by a "Contact support" row (`ContactSupportRow.tsx`, MailPen icon) opening `ContactSupportModal.tsx` (LoginToInterpretModal skeleton; subject + message via new `AuthTextArea.tsx` + `maxLength` prop on `AuthField`); `contactSupport` server action in `profile/actions.ts` (getCurrentUser guard, `contactSupportSchema` in new `support-schemas.ts` — subject 3–200 / message 10–5000, pinned by test to the contract doc) POSTs `{subject, message}` to `POST /api/v1/support/contact`; identity comes from the JWT backend-side. Manual entry now lives on `/user/readings`: round ClipboardPlus button absolutely positioned right of the centered "◇ Filter Readings ◇" toggle in `ReadingsFilterPanel.tsx` (inline positioning per stale-dev-CSS rule, inline borderRadius per the open Chrome rounded-full finding), `sanctum-tip` tooltip "Enter existing reading" right-aligned inline so it can't clip at narrow viewports. Backend contract written: `docs/backend-contracts/support-tickets.md` (SUPPORT_EMAIL env, `reply_to` extension to `send_email()`, response codes, ~5/hour rate limit). Status: FE completed (tsc, 130 tests, build green). Verified in real Chrome (puppeteer-core, headed, throwaway profile + throwaway user `support-verify@example.com` registered directly on the local backend — UI registration is flag-disabled): row swap, modal open/Escape/scroll-lock, zod field errors, valid submit → mapped 404 copy (endpoint not built yet — graceful by design), round button 36px/borderRadius 9999px right of the still-centered toggle, tooltip visible + within viewport, click lands on /user/manual-reading; at 375px no horizontal scroll, modal fits (343px). Found+fixed during verification: React 19 resets uncontrolled inputs after a form action, wiping the typed message on a failed submit — action now echoes `values` back in `ContactSupportFormState` and the modal feeds them to new `defaultValue` props on `AuthField`/`AuthTextArea`. Safari pass + live submit path pending backend implementation.

- **2026-09-15** — `/code-review` (high) on the contact-support branch: 10 findings, 9 applied, 1 declined. Applied: (1) the manual-entry tooltip's inline `transform: none` was killing the shared `.sanctum-tip` slide-up — replaced by a `.sanctum-tip__label--end` modifier in `tarot.css` (right-aligned, `translateY` only); (2) the round button's inline positioning moved to Tailwind (`absolute right-0 top-1/2 -translate-y-1/2`) — the inline `borderRadius: 9999px` is **kept** as the hedge for the still-open 2026-09-11 Chrome `rounded-full` finding; (3) the React 19 uncontrolled-input reset logged in `bugs.md`; (4) new `src/components/SanctumModal.tsx` (portal, scroll-lock, Escape, backdrop, panel, sticky header with title/subtitle/close, `size` sm|lg) now wraps `ContactSupportModal`, `LoginToInterpretModal` and `InterpretationModal`; (5) `AuthField.tsx` exports `AuthFieldFrame` + `AUTH_CONTROL_CLASS`/`AUTH_CONTROL_STYLE`, and `AuthTextArea` composes them instead of duplicating the markup; (6) both form modals use `AuthErrorBanner`/`AuthSubmitButton`; (7)+(8) `AuthFormState<TValues = never>` gained the optional `values` echo on the base type, `ContactSupportFormState = AuthFormState<ContactSupportInput>`; (10) stray "a" typo in `support-tickets.md`. Declined: (9) `docs/contact-support.md` says `LifeBuoy` where `MailPen` shipped — left as-is by Pavlos. Status: completed (tsc clean, 130 tests, build green).

- **2026-09-15** — Manual-reading entry point reworked: the round `ClipboardPlus` icon button + "Enter existing reading" tooltip beside the readings-list filter toggle was judged too cryptic (Pavlos: users wouldn't understand "manual"/"existing"). Removed from `ReadingsFilterPanel.tsx` (with the `.sanctum-tip__label--end` CSS modifier it needed); replaced by a muted one-liner `ManualReadingHint` in `src/app/user/readings/page.tsx` — "Read with your own deck? Record that reading here" → `/user/manual-reading` — rendered under the list (after pagination) and in the empty state. `/user/manual-reading` itself unchanged. ADR-010 amended. Status: completed.

- **2026-09-15** — `/code-review` (high) on the password-reset branch: 10 findings, 8 applied, 2 declined. Applied: (1) `publicFetch` now catches network/`GNOSIS_API_BASE_URL` failures and returns a status-0 result like `authenticatedFetch` (shared `NETWORK_ERROR_MESSAGE`), so a backend hiccup on forgot/reset no longer throws into Next's error boundary; (4)+(5) `resetPassword` dropped its 422 → "password length" guess (a 422 can just as well be the token) — 400/410 still drive `tokenProblem` for the dead-link panel (the form owns that copy, so the redundant `error` strings went), everything else falls through to `result.message`; `410: "This link has expired."` added to `SAFE_MESSAGES`; (6)+(7) the two forgot-password action bodies collapsed into `requestPasswordResetEmail(email)` in `src/lib/password-reset.ts`, which uses `result.message` — the hardcoded 429 copy that contradicted `SAFE_MESSAGES` (and `email-service.md`) is gone; (8) new `src/components/AuthPageShell.tsx` (`AuthPageShell` default + `AuthErrorBanner`, `AuthSubmitButton`, `AuthFootnote`, `AuthLink`) replaces the copy-pasted header/card/banner/button markup in login-form, register, forgot-password and reset-password-form — classes verbatim, pages keep only their own form; (9) `auth-schemas.ts` hoists `emailField`, `passwordField`, `passwordsMatch`/`PASSWORDS_MATCH`; (10) CODE_INDEX: `LogoutButton` entry removed, new forgot/reset/profile files and schema/type exports added. Declined: (2) `clearAuthCookies()` on reset success is unconditional — checking that the browser session matches the reset target needs the backend to identify the account on 200 (not in the contract; frontend-only session), and signing the browser out after a reset done in it is the conservative default; (3) restoring a JS-free logout button — the "Depart"→"Renew the Seal" swap was a deliberate product choice on 2026-09-14, `ProfileNav` logout is on every page, and JS-disabled isn't a supported target for this client-heavy app. Status: completed (tsc clean, 124 tests, build green).
- **2026-09-17** — Dev stack now compose-driven: root `make dev` brings up Mongo + API via `backend/docker-compose.yml` (`up --wait`, API healthcheck added, project pinned to `gnosis`, volume pinned as `gnosis-esoterica-api_mongo_data`) and runs Next.js natively with `GNOSIS_API_BASE_URL=http://localhost:8001` exported; `make dev-down` / `make dev-logs` added. Root cause of the login ECONNREFUSED: native `make dev` read `backend/.env` whose `MONGODB_URI` used the compose hostname `mongodb`, and no Mongo ran on the host. Also: the monorepo move had changed the compose project name from `gnosis-esoterica-api` to `backend`, creating an empty volume; after briefly running on a copy (`gnosis_mongo_data`), the stack was pointed back at the original volume `gnosis-esoterica-api_mongo_data`, pinned via `name:` in the compose volume, not `external`, so raw `docker compose up` works without a pre-created volume (2026-09-20). `frontend/.env.example` added. `MONGODB_URI` is now required with no `config.py` default (2026-09-20): `backend/.env.example` sets the host-side `mongodb://localhost:27019`, the compose `api` service overrides it for the container, prod reads `.env.gnosis.prod`; `tests/conftest.py` and CI supply an unused placeholder. "No readings after the volume switch" turned out to be two dev superadmins one character apart: compose seeded `dev@gnosisesoterica.dev` (empty) while the real history lives under `dev@gnosisesoterica.de`; the seed command now uses the `.de` address (matching `dev_users.md`) and the empty `.dev` user was deleted; five empty anonymous Mongo `configdb` volumes left by removed containers were also removed (2026-09-20). Status: completed.

- **2026-09-20** — Significators: court royal, un-numbered life numbers, `cards: 0` removed (plan `docs/significators-court-royal-life-numbers-drop-cards-0.md`, ADR-011). New `lib/court-royals.ts` (12-royal Golden Dawn Book T table keyed by the decanate pip) and `getCourtRoyal()`; `SignificatorResult.courtRoyal` and a `court royal` position (5 positions now, up to 7 cards). Life number cards keep `life number 1..N` as internal keys only — `buildReadingPayload` sends plain `life number` for Significators, so the backend and LLM see no numbers; the saved-reading page re-keys repeats via `uniquePositionKeys`, and legacy `life number 1/2` rows still render. New shared `lib/significator-positions.ts`; `SpreadCards` gained a grouped significators layout ("Life Numbers" banner); `app/significators/page.tsx` now renders `SignificatorsLayout` instead of a copy of its JSX. `ReadingConfig.cards` is optional; `isBirthdateSpread()` replaces every `cards === 0` check and the picker label reads "Significators (Birth Date)". Backend: no schema change; `dump_prompts.py` sample and `test_lean_prompt.py` fixtures updated, `prompt_reference.md` regenerated. Existing readings have no court royal and their interpretations never mention one (ADR-005: no re-roll). Each new chart adds one distinct card, about +150 words of server-owned budget. Status: completed (tsc clean, 181 frontend tests, 307 backend tests, ruff and prompt-doc-check green; manual browser pass pending).
- **2026-09-20** — Follow-up to the Significators change, from a screenshot of the interpretation modal: (1) the saved-chart banners collided ("DAY NUMBERSTAR SIGN") with all 7 cards in one row — the first layout used `gap-x-8`, a utility new to the app, against a long-running dev server (see the stale-dev-CSS rule); `SpreadCards` now lays the chart out in three rows (Day Number + Star Sign / Life Numbers / Decanate + Court Royal) using utilities already in use, with `whitespace-nowrap` banners and a gold rule. (2) The narrative rendered as one block with literal `**` markers although the stored text has blank-line paragraphs and bold per-card headings; `InterpretationDisplay` no longer relies on `whitespace-pre-wrap` — new `lib/narrative-blocks.ts` (`parseNarrative`, `splitBold`) turns it into heading + paragraph sections, checked against the six latest stored narratives. Status: completed (tsc clean, 185 tests; browser pass pending).
- **2026-09-20** — Life number cards now carry a per-card note in `position_description`. With un-numbered `life number` positions the LLM received three identical descriptions and improvised distinctions (e.g. a "Life Number as karmic structure" heading for Justice). `lifeNumberCardNote()` in `lib/significator-conversion.ts` states the real numerology: the card bears the life number itself, is its single-digit root, or "is number N, which reduces to the same root, R" (The Fool counts as 22). Descriptions stay under the 500-char cap (existing test). Applies to new charts only. Status: completed (tsc clean, 188 tests).
- **2026-09-20** — Mobile pass on the saved Significators layout and the new narrative rendering. At 375px the modal leaves about 291px of content width; three life number cards did not fit on one line because long names ("The High Priestess") widened their card. `SpreadCards` now puts each significator card in a fixed `w-20 sm:w-28` slot so names wrap under the card, with `gap-3` between cards, `gap-4` between groups and `px-2 sm:px-4` on groups (all utilities already in use). Verified in headless Chrome via a throwaway preview page, at a true 375px (same-origin iframe, since headless Chrome clamps the window to about 500px) and at 1100px: paired rows sit side by side, three life numbers fit on one line, headings and paragraphs wrap inside the panel, no horizontal overflow. Preview page removed. Safari and the real modal behind login not checked. Status: completed (tsc clean, 188 tests).
- **2026-09-21** — "New reading" button on the readings list: round 36px gold `PlayingCards` link to `/reading` in `ReadingsFilterPanel.tsx`, top right on the same line as the centred "Filter Readings" toggle, with the `sanctum-tip` tooltip (same icon, label and tooltip as the profile page's action). `.sanctum-tip__label--end` re-added to `tarot.css` (right-anchored, slides on Y only) so the label can't clip off a phone viewport. Gotcha found by rendering it: `.sanctum-tip { position: relative }` is unlayered CSS and beats Tailwind's layered `absolute`, so putting the positioning utilities on the link itself dropped the button to the left; the positioning lives on a wrapper `div` instead. Verified in headless Chrome via a throwaway preview page (tooltip forced visible) at a true 375px and at 1100px: toggle stays centred, button right-aligned and vertically centred, tooltip inside the viewport. Preview removed. ADR-010 amended. Status: completed (tsc clean, 191 tests; Safari and the real page behind login not checked).
- **2026-09-21** — New-reading button rendered as a pill in Pavlos's browser although headless Chrome drew it round: its size came from `w-9 h-9` utilities. Size and shape are now fully inline (`width`/`height`/`minWidth` 2.25rem, `padding: 0`, `flexShrink: 0`, `borderRadius: 50%`), so the circle does not depend on the served Tailwind CSS. Cause of the pill not confirmed. Status: completed (tsc clean, 191 tests).
- **2026-09-21** — Tooltips off on touch devices, app-wide: the `.sanctum-tip` show rules in `tarot.css` (the only themed tooltip; used by the profile page's two actions and the readings-list New reading button) now sit inside `@media (hover: hover) and (pointer: fine)`. On phones and tablets a tap left `:hover` stuck and the label lingered; there `aria-label` carries the name. An iPad with a trackpad or mouse reports a fine hovering pointer and still gets tooltips. Native `title` attributes (FaceUpDeck, AccountPanel, TarotGame) were left alone — browsers never show them on touch. Status: completed (tsc clean, 191 tests; not checked on a physical phone or tablet).
- **2026-09-21** — Fixed the flash of a stale view after "Done" in the interpretation modal (details in `bugs.md`): `InterpretationModal` now runs the parent's navigation in a transition and stays up until it commits. Status: completed (tsc clean, 191 tests, frame-by-frame browser check with an old-code control).
