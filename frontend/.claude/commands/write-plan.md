# write-plan

Write a plan file to /docs/{plan-name}.md, use the title of the plan as the filename. This file will be the base reference point 
for further iterations of the plan. File name given as ARGS. Add an ASCII sequence diagram with model changes at the most important points in the flow.