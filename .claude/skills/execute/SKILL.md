---
name: execute
description: >
  This skill will be used to execute an already written plan document in md form. The plan will have bee written previously and should
  have enough detail for the execution to give a bug free and clean implmentation. When analysing the plan the agent should look to 
  existing tests and those new tests that are required. The agent should use a TDD methodolgy such that new code has test coverage. 
  The agent should also make a final review before implementation of each segment of the plan and flag any risks or hidden flaws with
  the plan. 
---